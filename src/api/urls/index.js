import commonUrl from './common.url.js';
import articleUrl from './article.url.js';
import readUrl from './read.url.js';
const urls = Object.assign({},
  commonUrl,
  articleUrl,
  readUrl
);

export default urls;
